{
    "$schema": "https://turbo.build/schema.json",
    "pipeline": {
        "build": {
            "dependsOn": ["^build"],
            "outputs": ["build/**"]
        },
        "lint": {},
        "format": {}
    },
    "globalEnv": [
        "NODE_ENV",
        "HOST",
        "PORT",
        "JWT_SECRET_KEY",
        "DB_CONNECTION_STRING",
        "IPFS_CLUSTER_BASE_URL",
        "IPFS_CLUSTER_AUTH_PASSWORD"
    ]
}
